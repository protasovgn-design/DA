def missing_number(nums): # 1. Определяем, какое максимальное число должно быть в ряду. # Так как одного числа не хватает, n — это длина списка + 1. # В примере [1, 2, 3, 4, 5, 6, 8, 9, 10, 11] длина 10, значит n = 11. n = len(nums) + 1

# 2. Вычисляем "идеальную" сумму всех чисел от 1 до n по формуле.
# Используем целочисленное деление // 2, чтобы результат был целым числом.
expected_sum = (n * (n + 1)) // 2

# 3. Считаем сумму чисел, которые у нас есть в списке.
actual_sum = sum(nums)

# 4. Разница — это и есть наше потерянное число.
return expected_sum - actual_sum
